#toolbar
    #settings
        button(data-action='circles') Circles
    #actions
        button(data-action='')
            i.fa.fa-recycle
        button(data-action='')
            i.fa.fa-video-camera
        button(data-action='')
            i.fa.fa-undo
        button(data-action='')
            i.fa.fa-repeat
        button(data-action='')
            i.fa.fa-share
        button(data-action='')
            i.fa.fa-user